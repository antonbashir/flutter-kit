import 'package:flutter/material.dart';

class CommonThemeColors {
  static const success = Color(0xFF26DA72);
  static const error = Color(0xFFFF4747);
  static const warning = Color(0xFFFFCA00);
  static const info = Color(0xFF4075FF);
  static final insertedTextSelectionColor = Colors.green.withOpacity(0.2);
  static final deletedTextSelectionColor = Colors.red.withOpacity(0.2);
  static const disabledOpacity = 0.38;
  static const highlightOpacity = 0.12;

  CommonThemeColors._();
}

const searchBarElevation = 6.0;
